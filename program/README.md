# Line-Bridge / 线之枢纽 / 線の橋

一个使用 **Python 3.10 + pygame** 编写的数学益智小游戏。

## 运行
```bash
cd program
pip install pygame
python start.py
```

## 操作
- 鼠标点击/拖拽：创建点与线
- Z：撤销
- H：提示（每关两条）
- E：创建一个示例自定义关卡
- Esc：退出

## 打包
```bash
cd program
Model.cmd
Package.cmd
```
会在 `dist/` 下生成可执行文件。
